    <h1>Page Profil</h1>

    <p>Bienvenue sur la page d'ajout d'un avatar pour le User portant l'id : <%=@user.id%></p>
    <h3>Avatar actuel</h3>
    <%if @user.avatar.attached?%>
      <%= image_tag @user.avatar, alt: 'avatar', class:'img-fluid rounded-pill', style:'height:10rem ; width:10rem' %>
    <%else%>
      <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
    <%end%>

    <%= form_tag profile_avatars_path(@user), multipart: true do %>
      <%= file_field_tag :avatar %>
      <%= submit_tag "uploader l'avatar" %>
    <% end %>